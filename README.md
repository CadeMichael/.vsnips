# .vsSnips

snippets written in .json files with the same formatting as VS Code snippets. for use with vsnips or with coc.nvim via the setting 

```json
{
    "snippets.priority": 90,
    "snippets.textmateSnippetsRoots": ["~/.vsSnips"]
}
```
